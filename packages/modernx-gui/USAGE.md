# ModernX GUI v1.3.0 使用指南

## 🚀 新功能

### 1. Logger 集成
- 实时日志显示
- 支持不同日志级别 (info, warn, error, action, state)
- 日志导出功能
- 自动滚动

### 2. 模型编辑器
- 在线编辑模型 State、Effects、Reducers
- 实时保存和重置
- 代码高亮编辑器

### 3. 增强的 UI
- 现代化设计
- 渐变背景和毛玻璃效果
- 响应式布局
- 动画和过渡效果

## 📦 安装和启动

### 1. 启动 GUI
```bash
cd /Users/perlinson/taro/demo-project
npx modernx-gui@1.3.0
```

### 2. 启动测试应用
```bash
cd /Users/perlinson/taro/demo-project
node src/index.js
```

## 🎯 功能说明

### 仪表板
- 显示实时统计数据
- 状态概览
- 连接时长监控

### 状态监控
- 完整状态树显示
- 实时状态更新
- JSON 格式化显示

### 动作历史
- 所有 action 记录
- 时间戳显示
- Payload 详细信息

### 模型详情
- 每个模型的完整状态
- 复制功能
- 状态计数显示

### Logger 日志
- 实时日志流
- 颜色编码的日志级别
- 导出和清空功能

### 模型编辑器
- **State 编辑**: JSON 格式，实时验证
- **Effects 编辑**: JavaScript 代码编辑
- **Reducers 编辑**: JavaScript 代码编辑
- **保存功能**: 实时更新模型

## 🔧 技术实现

### Logger 集成
```javascript
// 发送日志到 GUI
function sendLog(level, message, data = null) {
  if (ws && ws.readyState === 1) {
    ws.send(JSON.stringify({
      type: 'logger',
      level,
      message,
      data
    }));
  }
}
```

### 模型编辑
```javascript
// 保存模型
saveModel() {
  const newState = JSON.parse(document.getElementById('state-editor').value);
  
  this.ws.send(JSON.stringify({
    type: 'save-model',
    payload: {
      namespace: this.currentEditingModel.namespace,
      state: newState,
      effects: document.getElementById('effects-editor').value,
      reducers: document.getElementById('reducers-editor').value
    }
  }));
}
```

## 🎨 UI 特性

- **渐变背景**: 紫色到蓝色的渐变
- **毛玻璃效果**: 半透明背景和模糊效果
- **动画过渡**: 平滑的悬停和切换动画
- **响应式设计**: 适配不同屏幕尺寸
- **图标系统**: Font Awesome 图标

## 📋 使用场景

1. **开发调试**: 实时查看状态变化
2. **性能分析**: 监控动作执行
3. **模型设计**: 可视化编辑模型
4. **日志分析**: 集中查看应用日志
5. **团队协作**: 共享状态和日志

## 🚧 未来计划

1. **CLI 集成**: 与 `modernx-cli` 深度集成
2. **文件保存**: 自动保存模型到文件
3. **热重载**: 编辑后自动重载应用
4. **插件系统**: 支持自定义插件
5. **主题系统**: 多种主题选择

## 📞 支持

如有问题或建议，请：
1. 查看 GitHub Issues
2. 提交新的 Issue
3. 联系开发团队

---

**ModernX GUI v1.3.0** - 让 ModernX 开发更简单！
